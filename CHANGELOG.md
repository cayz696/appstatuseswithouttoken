# AppTrack — Патч-ноути

> Дашборд для відстеження статусів мобільних додатків та тижневих KPI.
> Один файл `index.html` (HTML + CSS + JS, без бекенду). Дані тягнуться з Google Sheets по опублікованих CSV-посиланнях, без токенів і без API-ключів.

---

## Версія 1 — Базовий реліз

Перша задокументована версія. Опис того, **що вже є і як воно працює**.

### 🎯 Загальна ідея

AppTrack — це односторінковий дашборд (SPA) для команди, яка заливає додатки в сторі й рахує тижневий KPI. Уся логіка виконується у браузері:

- дані читаються напряму з двох листів Google Sheets (**Apps** і **Rental**), опублікованих як CSV;
- жодних серверів, баз даних чи токенів — усе працює локально у вкладці;
- налаштування (CSV-посилання, пароль) зберігаються в `localStorage` браузера.

### 🔐 Вхід і пароль

- При першому вході екран пропонує **встановити пароль** (мінімум 4 символи). Він зберігається в `localStorage` (ключ `_atpwh`).
- Наступні входи звіряють введений пароль зі збереженим.
- Пароль сесії тримається в `sessionStorage` (`_atpw`) — поки вкладка відкрита, повторно вводити не треба.
- ⚠️ Це «клієнтський» захист від випадкового відкриття, а не серйозна безпека: пароль зберігається у відкритому вигляді в браузері.

### 🔌 Підключення даних

- У блоці **⚙ Налаштування** вводяться два CSV-URL:
  - **Apps** — назви, акаунти, версії, бренд, статус (FDs);
  - **Rental** — дати App review / App live / App dead, Expenses, Lifetime, Paid weeks, Comments.
- Джерела беруться через `Файл → Поділитися → Опублікувати в інтернеті → CSV`.
- Посилання зберігаються в `localStorage` (`at5_u1`, `at5_u2`) і при наступному відкритті **дані вантажаться автоматично**.
- Якщо пряме завантаження блокується CORS — є автоматичний fallback через `corsproxy.io`.
- Власний CSV-парсер підтримує лапки й коми всередині значень.

### 🔄 Автооновлення

- Селектор **Авто**: вимк. / 5 / 10 / 30 хв (за замовчуванням — 5 хв).
- «Живий» індикатор (зелена крапка) і час останнього оновлення.

### 🧮 Логіка обробки (merge)

- **Дедуплікація Apps**: записи об'єднуються за `Bundle id` (або за назвою), лишається найвища версія.
- **Зшивання з Rental**: спершу за `Bundle id`, потім fallback за назвою.
- **Фільтр по даті**: у розрахунок беруться лише додатки з датою (review або live) **від 01.03.2026** (`CUT`).
- **Менеджер** визначається за префіксом акаунта: `(V)` або `V(self)` → **Влад**, `(O)` → інший, решта → **Саня**.
- **Self-акаунти** (містять `(self)`) — власні, без орендної плати: expenses і paid weeks у них завжди 0.

### 🏷 Класифікація версій (сіра / біла, поріг залежить від дати)

Функція `pVers` розбирає значення версії з урахуванням дати релізу:

- до **08.05.2026** (`G1_START`): «сіра» версія — це `≥ 3`;
- від **08.05.2026**: поріг знижено — «сіра» це `≥ 2` (спринт G1).

Типи:
- `gp` — 🟢 Сіра LIVE;
- `gm` — ⏳ Сіра MOD (значення з `?` на кінці — на модерації);
- `wp` — 🟦 Біла LIVE (версія 1+);
- `wm` — 🔶 Біла MOD;
- `dr` — 📝 не залито / чернетка.

### ⭐ Що таке KPI

Додаток зараховується в KPI, якщо він:
1. сіра LIVE-версія (`gp`),
2. має дату App live,
3. **прожив ≥ 1 дня** (живий зараз або lifetime ≥ 1).

Self-акаунти зараховуються в KPI, але **не впливають на середню вартість** (бо не мають витрат).

### 📅 Періоди / фільтри часу

Реалізовано кілька способів вибору періоду (усе перераховує дашборд):

- **Тиждень звіту** — стандартний `<input type="week">` + список пігулок наявних тижнів;
- **Місяць** — кнопки «поточний» / «попередній»;
- **Довільний діапазон** — два поля дат + «Застосувати» / «Скинути»;
- **Всі тижні** — весь період від 01.03.2026.

### 📊 Що показує дашборд

- **3 KPI-картки**: загальний KPI за період (з ціллю), окремо Влад і Саня (ціль ділиться ~50/50). Ціль задається полем «КПІ ціль: сірих / місяць» (за замовчуванням 60).
- **Ряд статистики (11 плиток)**: KPI, живих зараз, сірих на модерації, білих LIVE, dead за період, всього, середня вартість KPI, Rental (всього / KPI), Self (всього / KPI).
- **🎯 Воронка релізу**: 4 етапи — взято в роботу → опубліковано → сірі релізи → KPI, з конверсією між кроками.
- **🟢 Живуть прямо зараз** і **📅 Залиті цього тижня** — два списки з тегами менеджера й self.
- **📱 Всі додатки** — головна таблиця з фільтрами (за версією, менеджером, KPI, self, живі/dead), пошуком і сортуванням по колонках.
- **📅 Тижневі звіти** — авто-таблиця по всіх тижнях: KPI, Влад, Саня, dead, expenses, бан-рейт, % цілі.
- **🚫 Причини бану** — топ причин з коментарів dead-додатків.
- **💰 Фінанси за період** — витрати (усього / Влад / Саня), paid weeks, середні expenses на KPI, середній lifetime, бан-рейт.
- **👥 По менеджерах** — зведена таблиця показників на менеджера.
- **💀 Dead apps — деталі** — окрема таблиця мертвих додатків з причинами й сортуванням.

### 💵 Модель витрат

`getExpensesInPeriod` розподіляє суму `Expenses` рівномірно на `Paid weeks + 1` платежів (перший — у день заливки, далі щотижня) і рахує лише ті, що потрапляють у вибраний період. Self-акаунти витрат не генерують.

### 🎨 Інтерфейс

- Темна тема, шрифти Inter + Space Grotesk (Google Fonts).
- Кольорове кодування статусів (зелений/жовтий/синій/помаранчевий/червоний/бірюзовий).
- Адаптивна верстка (сітки перебудовуються на вузьких екранах).
- Мова інтерфейсу — українська.

---

### 🧱 Технічний стек

| Аспект | Реалізація |
|---|---|
| Формат | Один файл `index.html` |
| Залежності | Тільки Google Fonts (CDN), решта — інлайн |
| Дані | Google Sheets → опубліковані CSV |
| Зберігання | `localStorage` + `sessionStorage` |
| Бекенд | Немає |
| CORS fallback | `corsproxy.io` |

### ⚠️ Відомі обмеження версії 1

- Пароль зберігається у відкритому вигляді в браузері (не для реальної безпеки).
- Немає експорту (CSV / PDF / друк).
- Класифікація залежить від акуратності заповнення таблиць (префікси акаунтів, формат дат, `?` у версіях).
- Логіка порогів (`CUT`, `G1_START`) прописана константами в коді.
