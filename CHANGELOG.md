# AppTrack — Патч-ноути

> Дашборд для відстеження статусів мобільних додатків та тижневих KPI.
> Один файл `index.html` (HTML + CSS + JS). Дані тягнуться з Google Sheets по опублікованих CSV-посиланнях, без токенів і без API-ключів.

Поточна версія: **5.4.6** (25.09.2026)

---

## 5.4.6 — Когорта: мертві окремо від модерації — 25.09.2026

У таблиці когортної ефективності кожен self-додаток тижня потрапляє в один стан: став сірим, мертвий або ще на модерації. Мертві більше не рахуються як «ще на модерації». Додано колонки **Мертві** і **Смертність**. Сума трьох станів дорівнює «Взято».

---

## 5.4.5 — Підказки карток — 25.09.2026

На дашборді в кожної картки і в секції когортної ефективності з’явився текст, що саме рахується. «Взято в роботу», стани під ним і «Живі зараз» рахують тільки self-додатки.

---

## 5.4.4 — КПІ по App Live, стани по App Review — 25.09.2026

Дві різні метрики більше не змішуються. Сторінку **Звіт** повернуто до попередньої логіки.

### КПІ за період
Рахується по даті **App Live**, не по даті заливу. Умова: версія без `?`, усі три галочки Offer + Text G + Graph G, і додаток прожив ≥ 1 день (немає App Dead або від live до dead минув щонайменше день). Влад і Саня діляться за тією ж умовою. Число не ділиться на «взято в роботу».

### Картки станів
Фільтр — дата **App Review** у вибраному тижні:
- Взято в роботу
- Біла на модерації (версія з `?`, App Live ще немає)
- Сіра на модерації (версія з `?`, App Live уже є, Text G і Graph G)
- Біла LIVE (версія без `?`, у сторі, сірі галочки стоять не всі)
- Відхилені (є App Dead)

### Когортна ефективність заливу
Окрема таблиця за останні 6 тижнів self-заливів: взято, стало сірими станом на сьогодні, конверсія, ще на модерації. Тиждень молодший за 21 день — «дозріває», старший — «фінально».

---

## 5.4.3 — Тижневий набір: взято, модерація, відхилені — 25.09.2026

Дашборд і сторінка **Звіт** рахують один і той самий тижневий набір. База — додатки з датою **App review** у вибраному тижні.

### Додано
- Ряд карток: **Взято в роботу**, **На модерації**, **Відхилені**, **У сторі**, **КПІ з взятих**. Наведення на картку пояснює, що пораховано.
- Підпис під рядом: скільки з узятих self і скільки оренда.
- **Відхилені** — є дата App dead або Lifetime від 1 дня. Колонка App Dead у таблиці лишається.
- Той самий ряд у підсумку сторінки **Звіт**.

### Змінено
- Великі картки **КПІ / Влад / Саня** як і раніше рахують сірі за датою **App live** цього тижня, не за датою review.
- **Живі зараз** винесені окремо: це всі тижні від 01.03.2026, вибраний тиждень на них не впливає.
- Таблиця менеджерів: колонка **Відхилені** і **Взято** замість Dead і бан-рейту.
- Список внизу називається **Відхилені** і збігається з карткою.

### Прибрано
- Воронку релізу, бан-рейт, дублі карток (маленьке КПІ, білі LIVE, сер. вартість, чотири картки Rental/Self).

---

## 5.4.2 — Дашборд: тижні, сіра, прибрані блоки — 07.09.2026

Правки головного дашборда. Self-сторінку не чіпали.

### Змінено
- Пігулки **Тижні** з пагінацією (по 8, найновіші першими), кнопка **Всі** завжди на місці.
- Картка **📊 Всього (період)** стоїть першою в рядку показників.
- Сіра LIVE для версії 1–2 лише якщо стоять **усі три** галочки: Offer + Text G + Graph G. Версія 3+ лишається сірою. КПІ як і раніше потребує lifetime ≥ 1 дня.

### Прибрано
- Блоки **Живуть прямо зараз**, **Залиті цього тижня**, **Тижневі звіти (авто)**.

---

## 5.4.1 — Self: сортування ярусів і правки таблиці — 17.08.2026

Уточнення сторінки Self-акаунтів. Дашборд не чіпали.

### Змінено
- Таблиця Фази 2 за замовчуванням сортується по **ярусу** (Declining → Burned → Fresh → Proven → Active). Клік по заголовку колонки змінює сортування.
- Пагінації немає: таблиця скролиться в межах екрана, шапка колонок лишається на місці.
- Воронка: модерація і прогрів злиті в один рядок **Фаза 1, ще без релізу**.
- Бейдж таблиці: `N живих · M термінованих · разом` (щоб не плутати з «17 живих» у воронці).
- Простій без дати більше не показує `9999д`; алерт простою лише якщо є реальна дата.

---

## 5.4.0 — Self-акаунти — 17.08.2026

Окрема сторінка аналітики власних Google Play акаунтів. Дашборд (Apps / Rental / Звіт) не змінювали.

### Додано
- Кнопка **🏠 Self-акаунти** в шапці (як **📊 Звіт**): воронка, алерти, таблиця Фази 2.
- Класифікація акаунтів: Фаза 0 (куплений), Фаза 1 (прогрів / ban / moder), Фаза 2 (після релізу, живий або термінований).
- Яруси Фази 2: Fresh / Proven / Declining / Burned / Active.
- Алерти: простій >5 днів, падіння live-rate за тиждень, Proven з вільною ємністю (<15 додатків).
- CSV account-tracker **вшитий у код**; сторінка тягне його сама.
- Проксі **`GET /api/self-tracker`** у `server/index.js`: сервер качає Google CSV і віддає браузеру (обхід CORS).

### Обмеження
- Проксі працює лише коли крутиться Node (`npm start` / Railway). На голому `index.html` або GitHub Pages tracker не завантажиться.
- Live-rate і лічильники додатків з’являються, якщо дашборд уже підтягнув Apps.

---

## 5.3.0 — Порт логіки v5_3

Версії за Offer / Text G / Graph G, менеджери, CORS. Зафіксовано комітом `7e627aa`.

---

## Версія 1 — Базовий реліз

Перша задокументована версія. Опис того, **що вже є і як воно працює**.

### 🎯 Загальна ідея

AppTrack — це односторінковий дашборд (SPA) для команди, яка заливає додатки в сторі й рахує тижневий KPI. Уся логіка виконується у браузері:

- дані читаються напряму з двох листів Google Sheets (**Apps** і **Rental**), опублікованих як CSV;
- жодних серверів, баз даних чи токенів — усе працює локально у вкладці;
- налаштування (CSV-посилання, пароль) зберігаються в `localStorage` браузера.

### 🔐 Вхід і пароль

- При першому вході екран пропонує **встановити пароль** (мінімум 4 символи). Він зберігається в `localStorage` (ключ `_atpwh`).
- Наступні входи звіряють введений пароль зі збереженим.
- Пароль сесії тримається в `sessionStorage` (`_atpw`) — поки вкладка відкрита, повторно вводити не треба.
- ⚠️ Це «клієнтський» захист від випадкового відкриття, а не серйозна безпека: пароль зберігається у відкритому вигляді в браузері.

### 🔌 Підключення даних

- У блоці **⚙ Налаштування** вводяться два CSV-URL:
  - **Apps** — назви, акаунти, версії, бренд, статус (FDs);
  - **Rental** — дати App review / App live / App dead, Expenses, Lifetime, Paid weeks, Comments.
- Джерела беруться через `Файл → Поділитися → Опублікувати в інтернеті → CSV`.
- Посилання зберігаються в `localStorage` (`at5_u1`, `at5_u2`) і при наступному відкритті **дані вантажаться автоматично**.
- Якщо пряме завантаження блокується CORS — є автоматичний fallback через `corsproxy.io`.
- Власний CSV-парсер підтримує лапки й коми всередині значень.

### 🔄 Автооновлення

- Селектор **Авто**: вимк. / 5 / 10 / 30 хв (за замовчуванням — 5 хв).
- «Живий» індикатор (зелена крапка) і час останнього оновлення.

### 🧮 Логіка обробки (merge)

- **Дедуплікація Apps**: записи об'єднуються за `Bundle id` (або за назвою), лишається найвища версія.
- **Зшивання з Rental**: спершу за `Bundle id`, потім fallback за назвою.
- **Фільтр по даті**: у розрахунок беруться лише додатки з датою (review або live) **від 01.03.2026** (`CUT`).
- **Менеджер** визначається за префіксом акаунта: `(V)` або `V(self)` → **Влад**, `(O)` → інший, решта → **Саня**.
- **Self-акаунти** (містять `(self)`) — власні, без орендної плати: expenses і paid weeks у них завжди 0.

### 🏷 Класифікація версій (сіра / біла, поріг залежить від дати)

Функція `pVers` розбирає значення версії з урахуванням дати релізу:

- до **08.05.2026** (`G1_START`): «сіра» версія — це `≥ 3`;
- від **08.05.2026**: поріг знижено — «сіра» це `≥ 2` (спринт G1).

Типи:
- `gp` — 🟢 Сіра LIVE;
- `gm` — ⏳ Сіра MOD (значення з `?` на кінці — на модерації);
- `wp` — 🟦 Біла LIVE (версія 1+);
- `wm` — 🔶 Біла MOD;
- `dr` — 📝 не залито / чернетка.

### ⭐ Що таке KPI

Додаток зараховується в KPI, якщо він:
1. сіра LIVE-версія (`gp`),
2. має дату App live,
3. **прожив ≥ 1 дня** (живий зараз або lifetime ≥ 1).

Self-акаунти зараховуються в KPI, але **не впливають на середню вартість** (бо не мають витрат).

### 📅 Періоди / фільтри часу

Реалізовано кілька способів вибору періоду (усе перераховує дашборд):

- **Тиждень звіту** — стандартний `<input type="week">` + список пігулок наявних тижнів;
- **Місяць** — кнопки «поточний» / «попередній»;
- **Довільний діапазон** — два поля дат + «Застосувати» / «Скинути»;
- **Всі тижні** — весь період від 01.03.2026.

### 📊 Що показує дашборд

- **3 KPI-картки**: загальний KPI за період (з ціллю), окремо Влад і Саня (ціль ділиться ~50/50). Ціль задається полем «КПІ ціль: сірих / місяць» (за замовчуванням 60).
- **Ряд статистики (11 плиток)**: KPI, живих зараз, сірих на модерації, білих LIVE, dead за період, всього, середня вартість KPI, Rental (всього / KPI), Self (всього / KPI).
- **🎯 Воронка релізу**: 4 етапи — взято в роботу → опубліковано → сірі релізи → KPI, з конверсією між кроками.
- **🟢 Живуть прямо зараз** і **📅 Залиті цього тижня** — два списки з тегами менеджера й self.
- **📱 Всі додатки** — головна таблиця з фільтрами (за версією, менеджером, KPI, self, живі/dead), пошуком і сортуванням по колонках.
- **📅 Тижневі звіти** — авто-таблиця по всіх тижнях: KPI, Влад, Саня, dead, expenses, бан-рейт, % цілі.
- **🚫 Причини бану** — топ причин з коментарів dead-додатків.
- **💰 Фінанси за період** — витрати (усього / Влад / Саня), paid weeks, середні expenses на KPI, середній lifetime, бан-рейт.
- **👥 По менеджерах** — зведена таблиця показників на менеджера.
- **💀 Dead apps — деталі** — окрема таблиця мертвих додатків з причинами й сортуванням.

### 💵 Модель витрат

`getExpensesInPeriod` розподіляє суму `Expenses` рівномірно на `Paid weeks + 1` платежів (перший — у день заливки, далі щотижня) і рахує лише ті, що потрапляють у вибраний період. Self-акаунти витрат не генерують.

### 🎨 Інтерфейс

- Темна тема, шрифти Inter + Space Grotesk (Google Fonts).
- Кольорове кодування статусів (зелений/жовтий/синій/помаранчевий/червоний/бірюзовий).
- Адаптивна верстка (сітки перебудовуються на вузьких екранах).
- Мова інтерфейсу — українська.

---

### 🧱 Технічний стек

| Аспект | Реалізація |
|---|---|
| Формат | Один файл `index.html` |
| Залежності | Тільки Google Fonts (CDN), решта — інлайн |
| Дані | Google Sheets → опубліковані CSV |
| Зберігання | `localStorage` + `sessionStorage` |
| Бекенд | Немає |
| CORS fallback | `corsproxy.io` |

### ⚠️ Відомі обмеження версії 1

- Пароль зберігається у відкритому вигляді в браузері (не для реальної безпеки).
- Немає експорту (CSV / PDF / друк).
- Класифікація залежить від акуратності заповнення таблиць (префікси акаунтів, формат дат, `?` у версіях).
- Логіка порогів (`CUT`, `G1_START`) прописана константами в коді.
